version: "3.9"

services:

  backend:
    build: ./backend
    container_name: fastapi_backend
    restart: always
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    container_name: vite_frontend
    restart: always
    ports:
      - "80:80"
    depends_on:
      - backend